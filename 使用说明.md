# 使用说明

> 这份是**给人看的**。想深入看代码和踩过的坑，去 `README.md` 和 `05_技能/`。

## 这是什么

把两个地方的外部内容抓下来，**自动**整理成能直接看的文件夹和表格。

| 源 | 抓什么 | 花钱吗 |
|---|---|---|
| **灵造** | 小红书。按关键词搜、按博主深挖 | **花**。搜一次 20 积分；博主按条数 50 / 100 |
| **得到大脑** | 抖音订阅的博主、任意网页链接 | **不花**（会员制，只占次数）|

---

## 一张图

```
      灵造（小红书）                  得到大脑（抖音 / 任意网页）
      要花钱                          不花钱
           │                              │
           ▼                              ▼
 ① 采集   collect.py                     collect.py
           │                              │
           ▼                              ▼
 ② 储存   原始数据，原封不动存着            原始数据，原封不动存着
           │                              │
           ▼                              ▼
 ③ 加工   解析 → 统一字段 → 出表           解析 → 统一字段 → 出表
           │                              │
           └──────────┬───────────────────┘
                      ▼
 ④ 产出   04_产出/博主/<昵称_账号ID>/
            一条内容一个文件夹：逐字稿.md + 封面
            外加一张总的索引表
```

**采集和加工是自动接上的** —— 抓完它自己会整理，不用你记得做第二步。

### 为什么长这样

一开始只想抓小红书。后来加了得到大脑，发现两件事：

1. 两个源的数据形状完全不同，硬塞进一套代码会互相牵扯
2. 它们**脾气相反** —— 灵造的封面链接 3 小时就过期（要抢），得到大脑的永久有效

所以定了规矩：**一个源一个文件夹，各自独立**。
代价是代码有重复；换来的是**改一个碰不坏另一个，删掉一个就干净卸载**。

---

## 怎么用

### 不花钱那个（得到大脑）

```bash
# 第一步：列出有哪些博主，拿到 follow_id
python 01_采集/02_getnote/collect.py bloggers --topic <知识库ID>

# 第二步：抓这个博主的 20 条（含逐字稿和封面）
python 01_采集/02_getnote/collect.py details --topic <知识库ID> --follow <follow_id> --limit 20
```

跑完直接去 `04_产出/博主/<昵称>_<账号ID>/` 看 —— **一条内容一个文件夹，点进去就是逐字稿**。

`<知识库ID>` 从哪来？跑一句 `doctor` 就全列出来了：

```bash
python 01_采集/02_getnote/collect.py doctor
```

### 花钱那个（灵造）

```bash
# 先预演 —— 不加 --go 永远只是「说说要花多少」，不会真花钱
python 01_采集/01_lingzao/collect.py profile --url "<小红书主页链接>" --limit 20

# 看清楚花了多少，确认了再加 --go
python 01_采集/01_lingzao/collect.py profile --url "<小红书主页链接>" --limit 20 --go
```

**记住这一条就够了：不带 `--go` 的灵造命令，一分钱都花不出去。**

---

## 怎么维护

### 平时：跑一次自检

```bash
python 05_技能/selfcheck.py
```

它会替你查八件事：**有没有密钥泄露风险 / 封面缺多少 / 逐字稿有没有被截断 /
表里的数据和源数据对得上吗 / 文件命名有没有乱 / 配置有没有过期**。

**看到「错误 0」就是健康的。** 有「警告」通常是历史数据的老问题，不至于影响新抓的。

### 出问题了：照着查

| 现象 | 怎么办 |
|---|---|
| `04_产出` 里啥都没有 | 抓完没接上加工，手动跑一次（看 `README.md` 的命令表）|
| 每条逐字稿都一样长、都在 1200 字左右 | 抓到的字幕是**截断版**。检查 `02_储存` 里有没有 `*_完整字幕合集.md` |
| 封面缺很多（灵造） | 链接只活 3 小时，**过了就补不回来**，只能重新抓（花钱）|
| 得到大脑报 429 | 被限流了。把 `--gap` 调大（默认 3.5 秒），或分两批抓 |
| 报「凭证没填」 | 去 `01_采集/02_getnote/config.local.json` 填 |
| 连不上 GitHub | 代理的问题，跟代码无关 |
| 表格里一堆「未知」 | **那是正常的**，表示这个源没有这项数据 —— **别当 0 去算** |

### 要加一个新平台

**别自己发挥。** 看 `05_技能/新增渠道规范.md`，那是把「怎么加源」固化下来的流程，
照着走一遍就有验收清单。

---

## 三条边界（别越线）

1. **只抓外部内容**，不碰得到大脑里的私人笔记 —— 用户划的线
2. **标题一个字都不许改** —— 得到大脑会用 AI 改写标题，必须自己传原帖标题
3. **灵造花钱前要先说** —— 报一下预计花多少、余额剩多少

---

## 东西都在哪

| 想干什么 | 去哪 |
|---|---|
| 看抓回来的原始数据 | `02_储存/` |
| 看整理好的成品 | `04_产出/博主/` |
| 改抓取规则 | `01_采集/<源>/collect.py` |
| 改表格字段 | `03_加工/columns.md` |
| 查踩过的坑 / 操作手册 | `05_技能/` 和 `.workbuddy/skills/` |
| 加新平台 | `05_技能/新增渠道规范.md` |
| 备份 / 换电脑 | https://github.com/ANJIE2001/auto-crawl-router |

**`02_储存` 和 `04_产出` 不进 git**（数据太大）。换电脑要手动拷，或者重新抓。

---

## 一句提醒

**抓完记得看 `04_产出`。** 那里没东西，就等于这次没干活。
